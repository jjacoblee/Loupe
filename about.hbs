# Third-party notices

Loupe links the Rust crates below into its binaries. Each is used under the
license shown, and the full license text follows. This file is generated by
`cargo about` — see `about.toml`; do not edit it by hand.

Loupe itself is licensed under the Apache License 2.0. See `LICENSE` and
`NOTICE`.

Loupe also embeds Sublime Text syntax definitions and color themes by way of
the `two-face` crate. Those carry their own copyrights and are acknowledged
separately — run `loupe --acknowledgements` to print them.

## Summary

{{#each overview}}
- **{{name}}** — {{count}} crate(s)
{{/each}}

## Licenses

{{#each licenses}}
### {{name}}

Used by:
{{#each used_by}}
- [{{crate.name}} {{crate.version}}]({{#if crate.repository}}{{crate.repository}}{{else}}https://crates.io/crates/{{crate.name}}{{/if}})
{{/each}}

```
{{text}}
```

{{/each}}
